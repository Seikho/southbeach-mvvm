import knockout = require("./static/knockout");
import bootstrap = require("./static/bootstrap");
import public = require("./static/public");
import jquery = require("./static/jquery");
export = routes;

var routes = {
    public: public,
    jquery: jquery,
    knockout: knockout,
    bootstrap: bootstrap
}
